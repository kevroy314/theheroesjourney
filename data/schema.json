{
  "_comment": "Declarative schema for everything under data/. Read by two checkers with different reach: scripts/autoload/Content.gd validates the merged runtime view (and can run on a player's device), tools/validate_data.py validates the files on disk and additionally scans the GDScript source for the vocabularies below. It lives here rather than as a const in Content.gd because both readers need it and a hand-synced second copy would drift; data/*.json is also the only path the export presets ship, so this is the only place the device pass can read it from. Content.load_all() globs the subdirectories, never data/ itself, so this file is not mistaken for content.",
  "_paths": "Locator grammar, shared by 'files' and 'runtime'. A path is dot-separated segments: 'name' takes a field, 'name[]' takes a field and expands the list, 'name{}' takes a field and expands the dictionary's values, '[]' and '{}' on their own expand what the previous segment produced (so 'loot{}.[]' is every entry of every loot table), and a bare '.' means the record itself. A 'runtime' path's first segment names a property on Content. A path that selects nothing is reported: a locator typo otherwise validates an empty set and looks like a pass.",
  "vocabulary": {
    "_comment": "Words the engine implements. validate_data.py re-derives each of these from the GDScript source and fails if this list and the source disagree, so the schema cannot quietly go stale — that is the whole point of having a build-time tool as well as a runtime one.",
    "ops": [
      "set",
      "add",
      "mul",
      "min",
      "max"
    ],
    "when_keys": [
      "tier_gte",
      "tier_lte",
      "phase",
      "region",
      "kind",
      "axis",
      "zone_gte",
      "has_relic",
      "hp_below",
      "has_tag",
      "lacks_tag"
    ],
    "hooks": [
      "on_run_start",
      "on_area_enter",
      "on_area_clear",
      "on_task_complete"
    ],
    "hook_effects": [
      "grit",
      "item",
      "deadline",
      "log",
      "buff",
      "tag"
    ],
    "spite_effects": [
      "grit",
      "item",
      "steal_item",
      "trinket",
      "reveal_area",
      "hide_area",
      "deadline"
    ],
    "item_uses": [
      "keep_streak",
      "extend_deadline",
      "ward",
      "skip_to_threshold",
      "reveal_area",
      "full_scale",
      "guarantee_kind_spite",
      "reveal_echo",
      "resume_run",
      "finder"
    ],
    "item_where": [
      "home",
      "run",
      "auto"
    ],
    "wheel_reqs": [
      "axis_tasks",
      "all_axes_ring",
      "streak",
      "echoes_all",
      "loops",
      "ring",
      "anomalies",
      "warden"
    ],
    "screens": [
      "title",
      "menu",
      "palace",
      "gym",
      "workshop",
      "stores",
      "hearth",
      "observatory",
      "area",
      "overworld",
      "worldmap",
      "task",
      "boon",
      "event",
      "summary",
      "codex",
      "wheel",
      "inventory",
      "dialogue"
    ],
    "node_types": [
      "task",
      "free",
      "threshold",
      "echo",
      "cache",
      "mirror",
      "trinket",
      "spite",
      "warden",
      "choice"
    ],
    "loot_types": [
      "grit",
      "echo",
      "trinket",
      "item"
    ],
    "side_types": [
      "task",
      "echo",
      "cache",
      "mirror",
      "trinket",
      "spite"
    ],
    "movement_tokens": [
      "$same"
    ],
    "movement_token_prefixes": [
      "$choose:",
      "$other:"
    ],
    "unit_tokens": [
      "$set"
    ],
    "_comment_choice_inputs": "How an option on a choice node is answered. Absent means a button carrying the option's own words. \"text\" means the player types the answer, so the option renders a field as well as a button, and what they typed is what gets remembered rather than the tag.",
    "choice_inputs": [
      "text"
    ],
    "_comment_confirm_kinds": "How a task is confirmed once the plausibility wait is over: the deliberate act that says yes, I did that. One script each under scripts/game/gestures/, registered in HJGestures.KINDS. A movement may name one and so may an axis, which is where the default comes from — so adding a gesture is a new file, a line in KINDS, and a word here.",
    "confirm_kinds": [
      "hold",
      "tap_rapid"
    ],
    "_comment_truths": "An anomaly is a memory of a life adjacent to yours, and this is the claim it makes about itself. AreaScreen reads it into the line above the intro, so it is the frame the player is given before they act; nothing yet checks the claim, which is the point.",
    "truths": [
      "true",
      "false",
      "unclear"
    ],
    "_dynamic_key_fields": "Data fields whose *value* is a Rules key resolved at runtime rather than written as a literal in the source. Game.gd rolls loot with Rules.value(String(entry['scaled_by'])), so a key that only ever appears in a scaled_by field is genuinely read even though no literal call names it. validate_data.py cross-checks this list against the non-literal Rules.value() call sites it finds, so adding a second dynamic reader without listing it here is itself an error.",
    "dynamic_key_fields": [
      "scaled_by",
      "cost_key"
    ],
    "interactable_reach": [
      "on",
      "adjacent"
    ],
    "_comment_story": "Effects a dialogue line, a dialogue reply or an objective reward may carry. Objectives.apply_effects is the one implementation, and it hands 'grit', 'deadline' and the unnamed 'item' straight to Game.apply_effects rather than paying them a second way. Not yet reconciled against the source the way hook_effects is -- add `reconcile(report, schema, arms(objectives_gd, \"apply_effects\"), \"story_effects\", \"Objectives.apply_effects\")` to tools/validate_data.py to close that gap.",
    "story_effects": [
      "grit",
      "deadline",
      "resolve",
      "item",
      "tag",
      "objective",
      "log"
    ],
    "_comment_goals": "Completion conditions Objectives.progress() implements. Objectives.problems() checks the loaded data against this list at runtime.",
    "objective_goals": [
      "anomalies_in_ring"
    ],
    "_comment_critters": "The animal vocabularies. Each is reconciled against a `match` or a `const` in scripts/game/Critters.gd, the same way ops, when_keys and hook_effects are reconciled against Rules and Game -- so a goal the schema allows but the engine does not implement is an error here rather than an animal that stands perfectly still and looks like art direction.",
    "critter_goals": [
      "hold",
      "approach",
      "retreat",
      "wander"
    ],
    "critter_conditions": [
      "dist_gte",
      "dist_lte",
      "held_gte",
      "stray_gte",
      "has_tag",
      "lacks_tag"
    ],
    "critter_events": [
      "petted"
    ],
    "critter_targets": [
      "player",
      "home"
    ]
  },
  "types": {
    "themes": {
      "files": {
        "dir": "themes",
        "paths": [
          "."
        ]
      },
      "runtime": [
        "themes{}"
      ],
      "required": [
        "id",
        "name",
        "colors",
        "labels",
        "glyphs"
      ],
      "optional": [
        "desc",
        "unlocked_by_default",
        "unlock_cost",
        "mountain_line",
        "backdrops",
        "palace_backdrops",
        "palace_colors",
        "font_display"
      ]
    },
    "rulesets": {
      "files": {
        "dir": "rulesets",
        "paths": [
          "."
        ]
      },
      "runtime": [
        "rulesets{}"
      ],
      "required": [
        "id",
        "name"
      ],
      "optional": [
        "desc",
        "unlocked_by_default",
        "unlock_cost",
        "modifiers",
        "hooks"
      ]
    },
    "areas": {
      "files": {
        "dir": "areas",
        "paths": [
          "."
        ]
      },
      "runtime": [
        "areas{}"
      ],
      "required": [
        "id",
        "name",
        "nodes"
      ],
      "optional": [
        "intro",
        "outro",
        "slots",
        "truth"
      ],
      "enum": {
        "truth": "truths"
      }
    },
    "anomalies": {
      "_comment": "Anomalies are areas in every structural respect — same nodes, same slots — so the graph pass treats them together with areas and their ids share the area id space.",
      "files": {
        "dir": "content",
        "paths": [
          "anomalies[]"
        ]
      },
      "runtime": [
        "anomalies[]"
      ],
      "id_space": "areas",
      "required": [
        "id",
        "name",
        "tier",
        "weight",
        "nodes"
      ],
      "optional": [
        "intro",
        "outro",
        "slots",
        "truth"
      ],
      "enum": {
        "truth": "truths"
      }
    },
    "nodes": {
      "_comment": "One card in an area. `when` is the same condition dictionary a modifier carries, evaluated by Rules.passes against the run -- a node whose `when` fails is closed off, and HJAreaGen counts it as satisfied for everything downstream, so `select_next` is the flag that says so: the player picks a branch of an `exclusive_next` fork, the memory picks a branch of a `select_next` one, trying `next` in order and opening the first whose `when` passes. Every `select_next` fork needs a last branch with no `when` at all, or a run that matches none of them is offered nothing. `prompt`, `options` and `remember` belong to `choice` nodes: the prompt is the sentence the memory starts, the options finish it, and `remember` names the key the answer is filed under in Meta.self_description, which is the half of the answer that outlives the loop.",
      "files": {
        "dir": "areas",
        "paths": [
          "nodes[]"
        ],
        "also": [
          {
            "dir": "content",
            "paths": [
              "anomalies[].nodes[]"
            ]
          }
        ]
      },
      "runtime": [
        "areas{}.nodes[]",
        "anomalies[].nodes[]"
      ],
      "unique_ids": false,
      "required": [
        "id",
        "type"
      ],
      "optional": [
        "label",
        "text",
        "task",
        "grit",
        "loot",
        "next",
        "grants",
        "exclusive_next",
        "select_next",
        "side_of",
        "prompt",
        "options",
        "remember",
        "when"
      ],
      "enum": {
        "type": "node_types"
      },
      "refs": {
        "loot": "loot"
      }
    },
    "choice_options": {
      "_comment": "One answer to a choice node. `grants` is the existing node mechanism -- it writes to HJRun.tags, which Rules.passes already reads as has_tag/lacks_tag -- so an answer is a condition the rest of the content can consult with no new machinery. Grit belongs on the node rather than here: paying differently for different answers would price honesty, and the survey is the one place in the game that must not.",
      "files": {
        "dir": "areas",
        "paths": [
          "nodes[].options[]"
        ],
        "also": [
          {
            "dir": "content",
            "paths": [
              "anomalies[].nodes[].options[]"
            ]
          }
        ]
      },
      "runtime": [
        "areas{}.nodes[].options[]",
        "anomalies[].nodes[].options[]"
      ],
      "unique_ids": false,
      "required": [
        "text"
      ],
      "optional": [
        "grants",
        "grit",
        "input"
      ],
      "enum": {
        "input": "choice_inputs"
      }
    },
    "tasks": {
      "files": {
        "dir": "areas",
        "paths": [
          "nodes[].task"
        ],
        "also": [
          {
            "dir": "content",
            "paths": [
              "anomalies[].nodes[].task"
            ]
          }
        ]
      },
      "runtime": [
        "areas{}.nodes[].task",
        "anomalies[].nodes[].task"
      ],
      "required": [
        "movement",
        "units"
      ],
      "optional": [],
      "token_ref": {
        "movement": "movements"
      },
      "token_enum": {
        "units": "unit_tokens"
      }
    },
    "slots": {
      "files": {
        "dir": "areas",
        "paths": [
          "slots[]"
        ],
        "also": [
          {
            "dir": "content",
            "paths": [
              "anomalies[].slots[]"
            ]
          }
        ]
      },
      "runtime": [
        "areas{}.slots[]",
        "anomalies[].slots[]"
      ],
      "required": [
        "chance",
        "table"
      ],
      "optional": [
        "attach"
      ]
    },
    "slot_entries": {
      "files": {
        "dir": "areas",
        "paths": [
          "slots[].table[]"
        ],
        "also": [
          {
            "dir": "content",
            "paths": [
              "anomalies[].slots[].table[]"
            ]
          }
        ]
      },
      "runtime": [
        "areas{}.slots[].table[]",
        "anomalies[].slots[].table[]"
      ],
      "required": [
        "type",
        "w"
      ],
      "optional": [
        "label",
        "text",
        "movement",
        "units",
        "grit"
      ],
      "enum": {
        "type": "side_types"
      },
      "token_ref": {
        "movement": "movements"
      },
      "token_enum": {
        "units": "unit_tokens"
      }
    },
    "movements": {
      "files": {
        "dir": "movements",
        "paths": [
          "movements[]",
          "packs[].movements[]"
        ]
      },
      "runtime": [
        "movements{}"
      ],
      "required": [
        "id",
        "name",
        "axis",
        "unit",
        "set",
        "seconds_per_unit"
      ],
      "optional": [
        "cue",
        "scaling",
        "pack",
        "confirm"
      ],
      "refs": {
        "axis": "axes"
      },
      "enum": {
        "confirm": "confirm_kinds"
      }
    },
    "packs": {
      "files": {
        "dir": "movements",
        "paths": [
          "pack",
          "packs[].pack"
        ]
      },
      "runtime": [
        "packs{}"
      ],
      "required": [
        "id",
        "name",
        "cost"
      ],
      "optional": [
        "desc",
        "min_ring"
      ]
    },
    "echoes": {
      "files": {
        "dir": "echoes",
        "paths": [
          "echoes[]"
        ]
      },
      "runtime": [
        "echoes[]"
      ],
      "required": [
        "id",
        "order",
        "title",
        "text"
      ],
      "optional": []
    },
    "trinkets": {
      "files": {
        "dir": "content",
        "paths": [
          "trinkets[]"
        ]
      },
      "runtime": [
        "trinkets{}"
      ],
      "required": [
        "id",
        "name",
        "desc"
      ],
      "optional": [
        "modifiers",
        "hooks"
      ]
    },
    "items": {
      "files": {
        "dir": "content",
        "paths": [
          "items[]"
        ]
      },
      "runtime": [
        "items{}"
      ],
      "required": [
        "id",
        "name",
        "cost",
        "desc",
        "use",
        "where"
      ],
      "optional": [
        "amount"
      ],
      "enum": {
        "where": "item_where"
      }
    },
    "upgrades": {
      "files": {
        "dir": "content",
        "paths": [
          "upgrades[]"
        ]
      },
      "runtime": [
        "upgrades[]"
      ],
      "required": [
        "id",
        "name",
        "desc",
        "max_level",
        "cost",
        "modifiers"
      ],
      "optional": []
    },
    "rooms": {
      "files": {
        "dir": "content",
        "paths": [
          "rooms[]"
        ]
      },
      "runtime": [
        "rooms[]"
      ],
      "required": [
        "id",
        "name",
        "glyph",
        "cost",
        "screen",
        "desc",
        "adjacency"
      ],
      "optional": [
        "reveal_after",
        "revealed_by_default",
        "starter"
      ],
      "enum": {
        "screen": "screens"
      },
      "enum_extra": {
        "screen": [
          ""
        ]
      },
      "key_refs": {
        "adjacency": "rooms"
      }
    },
    "room_adjacency": {
      "files": {
        "dir": "content",
        "paths": [
          "rooms[].adjacency{}"
        ]
      },
      "runtime": [
        "rooms[].adjacency{}"
      ],
      "required": [
        "desc",
        "modifiers"
      ],
      "optional": []
    },
    "axes": {
      "files": {
        "dir": "content",
        "paths": [
          "axes[]"
        ]
      },
      "runtime": [
        "achievements.axes[]"
      ],
      "required": [
        "id",
        "name",
        "angle",
        "desc"
      ],
      "optional": [
        "confirm"
      ],
      "enum": {
        "confirm": "confirm_kinds"
      }
    },
    "wheel_rings": {
      "files": {
        "dir": "content",
        "paths": [
          "rings[]"
        ]
      },
      "runtime": [
        "achievements.rings[]"
      ],
      "required": [
        "ring",
        "count",
        "suffix",
        "resolve"
      ],
      "optional": []
    },
    "wheel_specials": {
      "files": {
        "dir": "content",
        "paths": [
          "special[]",
          "centre"
        ]
      },
      "runtime": [
        "achievements.special[]",
        "achievements.centre"
      ],
      "required": [
        "id",
        "name",
        "desc",
        "req",
        "resolve"
      ],
      "optional": [
        "modifiers"
      ],
      "req_type_vocab": "wheel_reqs"
    },
    "spite_alters": {
      "files": {
        "dir": "content",
        "paths": [
          "spite.alters[]"
        ]
      },
      "runtime": [
        "spite.alters[]"
      ],
      "required": [
        "id",
        "name",
        "line"
      ],
      "optional": []
    },
    "spite_events": {
      "files": {
        "dir": "content",
        "paths": [
          "spite.events[]"
        ]
      },
      "runtime": [
        "spite.events[]"
      ],
      "required": [
        "alter",
        "weight",
        "text",
        "effect"
      ],
      "optional": [],
      "refs": {
        "alter": "spite_alters"
      },
      "effect_vocab": "spite_effects"
    },
    "loot_entries": {
      "files": {
        "dir": "content",
        "paths": [
          "loot{}.[]"
        ]
      },
      "runtime": [
        "loot{}.[]"
      ],
      "required": [
        "type",
        "w"
      ],
      "optional": [
        "amount",
        "scaled_by"
      ],
      "enum": {
        "type": "loot_types"
      },
      "refs": {
        "scaled_by": "config_keys"
      }
    },
    "buffs": {
      "_comment": "A buff is a set of modifiers with an expiry and an icon, resolved through Rules like every other source. `hours` is a wall-clock timer; `break_on` lists events another system calls Buffs.trigger() with, and a buff with neither runs until something breaks it. `then` applies a second buff the moment this one ends, which is how coffee pays for itself. Not in `runtime`: Content.gd does not merge this key yet, so only the file-side tool checks these records — four lines in Content.load_all would fix that.",
      "files": {
        "dir": "content",
        "paths": [
          "buffs[]"
        ]
      },
      "runtime": [
        "buffs[]"
      ],
      "required": [
        "id",
        "name",
        "desc",
        "icon"
      ],
      "optional": [
        "hours",
        "break_on",
        "then",
        "modifiers",
        "apply_text",
        "expire_text"
      ],
      "refs": {
        "then": "buffs"
      }
    },
    "interactables": {
      "_comment": "Something in the world you can stand next to and act on. This is the catalogue — the verb, the price and what happens. The instances live in data/world/overworld.json as an `interactables` list of { x, y, type, label? } records, which is a shape the Tiled round trip already turns into a draggable object layer. `cost_key` names a config key rather than a number so a trinket can bend the price, and `effect` is one entry of the same vocabulary hooks use, applied by Game.apply_effects. A `critter` names a species in data/content/critters.json: the placement spawns that animal there, reach follows it as it walks, and `critter_event` (default \"petted\") is what acting on it fires at the state machine. That one key is why petting the dog and petting the cat do opposite things with no branch anywhere in the code. It is checked by critter_pass() in tools/validate_data.py rather than by `refs`: Content.gd builds its id sets from `runtime` paths and does not merge a `critters` key, so a `refs` entry here would resolve against an empty set and reject every placement on a player's device.",
      "files": {
        "dir": "content",
        "paths": [
          "interactables[]"
        ]
      },
      "runtime": [
        "interactables[]"
      ],
      "required": [
        "id",
        "name",
        "verb",
        "effect"
      ],
      "optional": [
        "blocks_until_tag",
        "cost_key",
        "critter",
        "critter_event",
        "desc",
        "icon",
        "once",
        "reach",
        "spent_text"
      ],
      "enum": {
        "reach": "interactable_reach",
        "critter_event": "critter_events"
      },
      "refs": {
        "cost_key": "config_keys"
      },
      "effect_vocab": "hook_effects"
    },
    "speakers": {
      "_comment": "Who can talk. `portrait` is a named seam: no art exists yet, and Dialogue.portrait_path() turns it into assets/portraits/<portrait>_<mood>.png the moment some does.",
      "files": {
        "dir": "dialogue",
        "paths": [
          "speakers[]"
        ]
      },
      "runtime": [
        "speakers{}"
      ],
      "required": [
        "id",
        "name",
        "portrait"
      ],
      "optional": [
        "accent",
        "desc"
      ]
    },
    "dialogues": {
      "files": {
        "dir": "dialogue",
        "paths": [
          "dialogues[]"
        ]
      },
      "runtime": [
        "dialogues{}"
      ],
      "required": [
        "id",
        "speaker",
        "start",
        "nodes"
      ],
      "optional": [
        "once"
      ],
      "refs": {
        "speaker": "speakers"
      }
    },
    "dialogue_nodes": {
      "_comment": "One beat: a few lines in order, then either `next` or `replies`. Node ids are unique within their own conversation, not across all of them -- every graph is allowed an 'end'. Dialogue.problems() checks that every next/goto/start names a node in its own graph, which is the half of the check this cannot see.",
      "files": {
        "dir": "dialogue",
        "paths": [
          "dialogues[].nodes[]"
        ]
      },
      "runtime": [
        "dialogues{}.nodes[]"
      ],
      "required": [
        "id",
        "lines"
      ],
      "optional": [
        "speaker",
        "mood",
        "next",
        "replies",
        "effects",
        "end"
      ],
      "refs": {
        "mood": "spite_alters",
        "speaker": "speakers"
      },
      "unique_ids": false
    },
    "dialogue_replies": {
      "_comment": "What the player says back. `when` is the same condition dictionary a modifier carries, evaluated by Rules.passes -- there is one condition language in this repo and this is it.",
      "files": {
        "dir": "dialogue",
        "paths": [
          "dialogues[].nodes[].replies[]"
        ]
      },
      "runtime": [
        "dialogues{}.nodes[].replies[]"
      ],
      "required": [
        "text"
      ],
      "optional": [
        "goto",
        "when",
        "effects"
      ],
      "unique_ids": false
    },
    "objectives": {
      "_comment": "What you are trying to do. Progress persists across runs; see scripts/autoload/Objectives.gd.",
      "files": {
        "dir": "objectives",
        "paths": [
          "objectives[]"
        ]
      },
      "runtime": [
        "objectives{}"
      ],
      "required": [
        "id",
        "title",
        "desc",
        "goal"
      ],
      "optional": [
        "giver",
        "reward"
      ],
      "refs": {
        "giver": "speakers"
      }
    },
    "objective_goals": {
      "files": {
        "dir": "objectives",
        "paths": [
          "objectives[].goal"
        ]
      },
      "runtime": [
        "objectives{}.goal"
      ],
      "required": [
        "type"
      ],
      "optional": [
        "ring"
      ],
      "enum": {
        "type": "objective_goals"
      },
      "unique_ids": false
    },
    "story_effects": {
      "files": {
        "dir": "dialogue",
        "paths": [
          "dialogues[].nodes[].effects[]",
          "dialogues[].nodes[].replies[].effects[]"
        ],
        "also": [
          {
            "dir": "objectives",
            "paths": [
              "objectives[].reward[]"
            ]
          }
        ]
      },
      "runtime": [
        "dialogues{}.nodes[].effects[]",
        "dialogues{}.nodes[].replies[].effects[]",
        "objectives{}.reward[]"
      ],
      "required": [
        "type"
      ],
      "optional": [
        "amount",
        "item",
        "count",
        "objective",
        "tag",
        "text",
        "kind",
        "silent"
      ],
      "enum": {
        "type": "story_effects"
      },
      "refs": {
        "objective": "objectives"
      },
      "unique_ids": false
    },
    "critters": {
      "_comment": "An animal that moves: a named state machine over the tile grid. `start` names the state it wakes in, `states` is a map of state id to what it does there, and `transitions` is an ordered list -- first match wins, so order is priority. Placement is world data, not here: an interactables record whose catalogue entry carries a `critter` spawns one where it stands, which is what lets a designer drag the cat into another room or retype the dog as a second cat with no code. Not in `runtime`: Content.gd does not merge a `critters` key yet, so only the file-side tool checks these records -- four lines in Content.load_all would fix that, exactly as the buffs comment says of buffs.",
      "files": {
        "dir": "content",
        "paths": [
          "critters[]"
        ]
      },
      "required": [
        "id",
        "name",
        "sprite",
        "start",
        "states"
      ],
      "optional": [
        "transitions"
      ]
    },
    "critter_states": {
      "_comment": "One state. `goal` is what it does while it is here; `keep` is the distance it is trying to hold, read as a floor by `retreat` and a ceiling by `approach`; `trail` aims it at where the target was N moves ago rather than where it is, which is the lag that makes a follower read as a companion rather than as a mirror; `pace` is seconds per cell as a multiple of critter.tick_seconds, so 1.0 is exactly the player's walking speed; `recover` is how many move attempts may make no progress before the animal abandons the path and reappears at the edge of its band, which is what stops a follower wedged behind furniture from being lost for the run. No `id`: the state's name is its key in the map.",
      "files": {
        "dir": "content",
        "paths": [
          "critters[].states{}"
        ]
      },
      "required": [
        "goal"
      ],
      "optional": [
        "keep",
        "trail",
        "pace",
        "recover",
        "restless",
        "say",
        "target"
      ],
      "enum": {
        "goal": "critter_goals",
        "target": "critter_targets"
      }
    },
    "critter_transitions": {
      "_comment": "One rule for leaving a state. `from` is a state id or a list of them, or \"*\" for any. `on` names an event fired at the animal by something acting on it; a rule with no `on` is tested every tick. `when` is a set of conditions, all of which must hold. `effect` is one entry of the same vocabulary hooks and interactables use, applied by Game.apply_effects -- which is how an escort that fails can set a run tag without this system knowing what a quest is. No `id`: a transition is identified by its position in the list, and its position is its priority. `effect` is checked by critter_pass() in tools/validate_data.py rather than by the generic effect_vocab hook, because that hook is written for a required field and reports a missing one as the type None -- and most transitions correctly have no effect at all.",
      "files": {
        "dir": "content",
        "paths": [
          "critters[].transitions[]"
        ]
      },
      "required": [
        "from",
        "to"
      ],
      "optional": [
        "on",
        "when",
        "effect"
      ],
      "enum": {
        "on": "critter_events"
      }
    }
  },
  "world": {
    "_comment": "data/world/overworld.json is not loaded by Content (World.gd reads it), so only the file-side tool checks it. The prop check is the one that has already bitten: a prop whose biome the generator never produces is correct, drawn, and invisible.",
    "required": [
      "w",
      "h",
      "ring_width",
      "max_tier",
      "centre",
      "spawn",
      "regions",
      "anomalies",
      "tiles_b64_deflate",
      "elevation_b64_deflate",
      "props_b64_deflate",
      "blocked_b64_deflate",
      "cliffs_b64_deflate",
      "terrace_levels"
    ],
    "region_refs": "areas",
    "anomaly_area_refs": "areas",
    "_scatter_excluded": "scatter_props() in tools/make_world.py refuses to place on these materials, so a prop declaring one of them as its biome can never appear even though the world is full of them.",
    "scatter_excluded_materials": [
      "path_dirt",
      "door",
      "bridge"
    ],
    "_prop_light": "Where light comes from. A prop entry in assets/tiles/tiles.json may carry a `light`, and that is the whole contract between the art side and the lighting pass: the art declares WHERE a source is and what colour it is, and never renders one. Nothing in the tileset reads the key back. A source declared wrong is invisible rather than wrong-looking, which is exactly the failure this file exists to catch, so all three fields are required once the key is present.",
    "prop_light": {
      "required": [
        "radius",
        "color",
        "flicker"
      ],
      "radius": "float greater than 0, in tiles",
      "color": "#RRGGBB, the colour at the source; the falloff belongs to the renderer",
      "flicker": "float 0.0 to 1.0, 0 is a steady lamp"
    },
    "_indoors": "Optional. { x, y, w, h } in cells, the footprint of the building the player wakes in, walls included. The Boon of the White Room is free walking inside it and breaks on the first cell outside, and that same crossing unlocks the Hearth and triggers the Spite encounter (Beat 5). Classified by shape as a marker with w and h as editable properties, so it is a rectangle the designer can drag rather than a rule derived from the floor material.",
    "_interactables": "Optional. A list of { x, y, type, label? } records naming an entry of the interactables catalogue in data/content/. Classified by shape as an object layer, so Tiled can place, drag and retype them with no tooling change. tools/validate_data.py does not yet check that `type` resolves — the world pass would need one loop, mirroring the anomaly `area` check."
  }
}
